'use strict';

window.warehouseSystem = window.warehouseSystem || {};

warehouseSystem.actions = (function() {
  /**
   * 暴露仓储管理动作集合。
   * @returns {Object} 当前为空对象。
   *
   * 原因：仓储子页面当前只展示模拟数据，保留 actions 命名空间便于后续新增出入库写操作。
   */
  return {};
})();
